# Metrics Export

This document details format exports.

## 1. Export JSON Format

- Downloads a raw JSON file containing all sections' metrics parameters.

---

## 2. Export CSV Format

- Formats metric sections, metric names, and current values as standard spreadsheet columns.
