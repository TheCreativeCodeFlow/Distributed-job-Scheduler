# Scheduler Module

This document outlines the Scheduler Operations Center.

## 1. Loop Latency & Settings

- Reports the active polling interval settings (default 5000ms loop delay).
- Tracks last execution timestamps and loop latency times.

---

## 2. Health Monitoring Panel

- Displays connectivity indicators for PostgreSQL databases, Redis memory nodes, active compute loop heartbeats, and worker pool densities.
