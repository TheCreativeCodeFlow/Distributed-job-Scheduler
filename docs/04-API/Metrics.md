# Metrics API

This document lists endpoints for fetching system performance metrics.

## 1. Endpoints

- `GET /v1/metrics/overview` (aggregate system statistics)
- `GET /v1/metrics/queues` (queue length and delay metrics)
- `GET /v1/metrics/workers` (execution rate and compute metrics)
