# Organizations API

This document lists endpoints managing tenancy and organization limits.

## 1. Endpoints

- `GET /v1/organizations`
- `POST /v1/organizations`
- `GET /v1/organizations/:id`
- `PATCH /v1/organizations/:id`
