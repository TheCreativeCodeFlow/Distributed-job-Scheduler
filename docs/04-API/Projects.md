# Projects API

This document lists endpoints for managing projects inside organizations.

## 1. Endpoints

- `GET /v1/projects`
- `POST /v1/projects`
- `GET /v1/projects/:id`
- `DELETE /v1/projects/:id`
