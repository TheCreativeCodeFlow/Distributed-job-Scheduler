# Queues API

This document lists endpoints for managing execution queues.

## 1. Endpoints

- `GET /v1/queues`
- `GET /v1/queues/:name`
- `POST /v1/queues/:name/pause`
- `POST /v1/queues/:name/resume`
